# Results

This folder is for output artifacts you generate by running the scripts in
`scripts/` against your own copy of the METLIN 960K corpus (not included in
this repository -- see main README for access).

Pre-computed results referenced throughout the manuscript are provided
directly in `data/processed/` rather than here, since they are themselves
inputs to figure generation and downstream analysis, not final outputs.
